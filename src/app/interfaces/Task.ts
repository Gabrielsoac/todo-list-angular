export type TTask = {
    id: string;
    userId: string;
    title: string;
    summary: string;
    dueDate: string;
}

export type TNewTask = {
  title: string;
  summary: string;
  date: string;
}
